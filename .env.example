# Briefings - Environment Variables Example
# Copy this file to .env and fill in your values
# 
# For production, use npx wrangler secret put for sensitive values:
#   echo "value" | npx wrangler secret put VARIABLE_NAME

# ============================================================================
# REQUIRED
# ============================================================================

# Google Gemini API Key (required for AI summarization)
# Get your key at: https://aistudio.google.com/app/apikey
# Set via: echo "your-key" | npx wrangler secret put GEMINI_API_KEY
GEMINI_API_KEY=your-gemini-api-key-here

# API Key for HTTP endpoint authentication (required)
# Used for all POST /api/run/* endpoints and other protected routes
# Must be provided in header: X-API-Key: your-api-key
# Set via: echo "your-key" | npx wrangler secret put API_KEY
API_KEY=your-secure-api-key-min-16-chars

# ============================================================================
# OPTIONAL - Email Delivery via Resend
# ============================================================================

# Resend API Key (optional - for weekly digest emails)
# Get your key at: https://resend.com/api-keys
# Set via: echo "your-key" | npx wrangler secret put RESEND_API_KEY
# RESEND_API_KEY=re_your_api_key_here

# Email From Address (must be verified domain in Resend)
# Set in wrangler.toml [vars] section or as env var
# EMAIL_FROM=briefings@yourdomain.com

# Email To Address (comma-separated for multiple recipients)
# Set in wrangler.toml [vars] section or as env var
# EMAIL_TO=you@example.com,team@example.com

# Email Subject Prefix (default: [Briefings])
# Prepended to weekly digest email subjects
# Examples:
#   [Briefings] → "[Briefings] 🥩 AI Red Flags, Cloud Wars..."
#   🥩 The Beef → "🥩 The Beef AI Red Flags, Cloud Wars..."
#   (empty) → "🥩 AI Red Flags, Cloud Wars..."
# Set in wrangler.toml [vars] section or as env var
# EMAIL_SUBJECT_PREFIX=[Briefings]

# ============================================================================
# OPTIONAL - Development & Configuration
# ============================================================================

# Environment (development | staging | production)
# Affects middleware behavior - development mode skips auth if API_KEY not set
# Set in wrangler.toml [vars] section
# ENVIRONMENT=production

# Log Level (debug, info, warn, error)
# Controls verbosity of logging output
# Set in wrangler.toml [vars] section
# LOG_LEVEL=info
